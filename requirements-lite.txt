# SysCRED - Requirements (Lite version for Render free tier)
# Minimal dependencies - NO Torch, NO ML models
# For showing IR metrics: Precision, Recall, BM25, TF-IDF, MAP, NDCG, MRR

# === Web Backend ===
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=20.1.0
python-dotenv>=1.0.0

# === Data Processing (for IR metrics) ===
pandas>=2.0.0

# === Development/Testing ===
pytest>=7.0.0
